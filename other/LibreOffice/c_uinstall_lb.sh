#!/bin/sh
sudo apt-get remove --purge libreoffice* && sudo apt-get clean && sudo apt-get autoremove && sudo apt clean && sudo apt autoclean && sudo apt autoremove && reboot

# commpletely uninstall LibreOffice
